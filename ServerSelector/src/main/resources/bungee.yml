name: ServerSelector
version: '${project.version}'
main: serverselectorplugin.serverselector.ServerSelector
description: "Bungeecord plugin to switch servers"

commands:
  openserver:
    description: "Switches the server, based on what was given"
    usage: "/openserver <target_server>"
